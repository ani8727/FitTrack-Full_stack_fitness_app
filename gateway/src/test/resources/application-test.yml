spring:
  application:
    name: gateway-test
  cloud:
    config:
      enabled: false
  main:
    web-application-type: reactive
    allow-bean-definition-overriding: true

server:
  port: 0

eureka:
  client:
    enabled: false

logging:
  level:
    org.springframework: WARN
