# Root env example — copy to .env for local development (do not commit secrets)

# ===== KEYCLOAK CLOUD (Cloud-IAM) =====
KEYCLOAK_ISSUER_URI=https://lemur-10.cloud-iam.com/auth/realms/fitness-auth
KEYCLOAK_REALM=fitness-auth
KEYCLOAK_BASE_URL=https://lemur-10.cloud-iam.com
KEYCLOAK_CLIENT_ID=fittrack-backend

# Other example entries (databases, brokers etc) follow the existing backend/.env format
