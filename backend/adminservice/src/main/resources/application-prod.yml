# Production-specific overrides
# Main configuration comes from config server

management:
  endpoints:
    web:
      exposure:
        include: health,info

spring:
  jpa:
    open-in-view: false
    show-sql: false
