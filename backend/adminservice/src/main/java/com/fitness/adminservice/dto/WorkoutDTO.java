package com.fitness.adminservice.dto;

// WorkoutDTO removed — no workout service in this deployment.
public class WorkoutDTO {
    // placeholder to avoid compilation errors if referenced accidentally
}
