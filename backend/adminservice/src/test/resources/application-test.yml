spring:
  application:
    name: adminservice-test
  config:
    import: ""
  cloud:
    config:
      enabled: false
    loadbalancer:
      enabled: false
    discovery:
      enabled: false
    service-registry:
      auto-registration:
        enabled: false
  datasource:
    url: jdbc:h2:mem:admin;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password: 
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    show-sql: false
  h2:
    console:
      enabled: false

eureka:
  client:
    enabled: false

logging:
  level:
    org.springframework: WARN
    org.hibernate.SQL: WARN
