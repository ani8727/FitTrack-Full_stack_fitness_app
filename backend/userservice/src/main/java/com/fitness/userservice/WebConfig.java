package com.fitness.userservice;

import org.springframework.context.annotation.Configuration;

@Configuration
public class WebConfig {
    // CORS is handled at the Gateway level - no need to configure here
}
