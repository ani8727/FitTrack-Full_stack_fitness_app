spring:
  application:
    name: ai-service
  config:
    import: optional:${SPRING_CONFIG_IMPORT:configserver:http://localhost:8888}

# If you later remove the Config Server, this file can be deleted safely.
