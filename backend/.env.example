## Environment variables template (do not commit secrets)

# This file is a template of environment variables used by the app.
# - Use `.env` for local development (copy from this file and fill values).
# - `.env.prod` is a sample production file with example values (DO NOT commit real secrets).
# Having both files is normal: `.env.example` documents required keys, `.env.prod` shows a production example.

# MongoDB Atlas (SRV URI) - used by activity/ai services
MONGO_URI=

# Neon PostgreSQL - used by user/admin services
POSTGRES_URL=
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=

# CloudAMQP (RabbitMQ)
RABBITMQ_HOST=
RABBITMQ_PORT=
RABBITMQ_USERNAME=
RABBITMQ_PASSWORD=
RABBITMQ_URI=
RABBITMQ_VIRTUAL_HOST=/
RABBITMQ_LISTENER_AUTO_START=true

# Local RabbitMQ docker defaults (optional)
RABBITMQ_DEFAULT_USER=
RABBITMQ_DEFAULT_PASS=

# Keycloak / OAuth2
KEYCLOAK_SERVER_URL=
KEYCLOAK_REALM=
KEYCLOAK_ADMIN=
KEYCLOAK_ADMIN_PASSWORD=
KEYCLOAK_ADMIN_CLIENT_ID=
KEYCLOAK_ISSUER_URI=
KEYCLOAK_JWK_SET_URI=

# Notes on duplicates:
# - Some configs in the repository may reference legacy service-specific env names (e.g. ACTIVITY_MONGODB_URI, AI_MONGODB_URI).
#   Prefer the unified `MONGO_URI` variable instead.
# - Prefer using the global names above (`MONGO_URI`, `POSTGRES_URL`, etc.) across Config Server files
#   to avoid duplication. If you need service-specific overrides, use SERVICE_PREFIX_VAR (optional).
# - Do not commit real secrets. Keep `.env` and `.env.prod` out of version control.

# Service-specific ports (change these in your `.env` or `.env.prod` to affect each service)
# Example: to change the Activity Service port, set `ACTIVITY_SERVICE_PORT=8082`
ACTIVITY_SERVICE_PORT=8082

# AI Service port
AI_SERVICE_PORT=8084

# Admin, User, Gateway service ports
ADMIN_SERVICE_PORT=8083
USER_SERVICE_PORT=8081
GATEWAY_SERVICE_PORT=8085

# Eureka (service discovery)
EUREKA_DEFAULT_ZONE=http://eureka:8761/eureka/
EUREKA_HOST=eureka
EUREKA_PREFER_IP=false

# Shared Spring Cloud Config import (optional)
SPRING_CONFIG_IMPORT=configserver:http://configserver:8888

# RabbitMQ exchange/queue names used by activity service
RABBITMQ_EXCHANGE=activity.exchange
RABBITMQ_QUEUE=activity.queue
RABBITMQ_ROUTING_KEY=activity.tracking

# Optional per-service overrides (only set if you need separate values)
# AI service can reuse the same exchange/queue names or set its own below.
AI_RABBITMQ_EXCHANGE=
AI_RABBITMQ_QUEUE=
AI_RABBITMQ_ROUTING_KEY=

# Observability
SENTRY_DSN=
SENTRY_TRACES_SAMPLE_RATE=1.0

# Misc runtime settings
CORS_ALLOWED_ORIGINS=
SPRING_PROFILES_ACTIVE=local

# AI integrations
GEMINI_API_URL=
GEMINI_API_KEY=

# Database tuning defaults
JPA_HIBERNATE_DDL_AUTO=update
JPA_SHOW_SQL=false
