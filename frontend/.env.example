# Frontend (Vite) env examples
# Copy to .env.local for local development.

# Production API base (override locally in .env.local for dev)
VITE_API_BASE_URL=https://api.fittrack.com
# Cloud-managed Keycloak (Cloud-IAM)
VITE_KEYCLOAK_BASE_URL=https://lemur-10.cloud-iam.com
VITE_KEYCLOAK_REALM=fitness-auth
VITE_KEYCLOAK_CLIENT_ID=fittrack-frontend
VITE_REDIRECT_URI=https://app.fittrack.com
